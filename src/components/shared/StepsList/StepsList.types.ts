/**
 * Props for the StepList component
 * @property stepsList  - list of step descriptions
 * @property currentStep - current selected step
 */
export type StepListProps = {
  stepsList: string[];
  currentStep: number;
};
